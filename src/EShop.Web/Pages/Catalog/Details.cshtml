@page "{id:int}"
@model EShop.Web.Pages.Catalog.DetailsModel
@{
    ViewData["Title"] = "Catalog Item Details";
}

<div class="container">
    <h1>Catalog Item Details</h1>

    @if (Model.Item != null)
    {
        <div class="row">
            <div class="col-md-4">
                <img src="/Pics/@Model.Item.PictureFileName" alt="@Model.Item.Name" class="img-fluid" />
            </div>
            <div class="col-md-8">
                <dl class="row">
                    <dt class="col-sm-3">Name</dt>
                    <dd class="col-sm-9">@Model.Item.Name</dd>

                    <dt class="col-sm-3">Description</dt>
                    <dd class="col-sm-9">@Model.Item.Description</dd>

                    <dt class="col-sm-3">Price</dt>
                    <dd class="col-sm-9">@Model.Item.Price.ToString("C")</dd>

                    <dt class="col-sm-3">Brand</dt>
                    <dd class="col-sm-9">@Model.Item.CatalogBrand?.Brand</dd>

                    <dt class="col-sm-3">Type</dt>
                    <dd class="col-sm-9">@Model.Item.CatalogType?.Type</dd>

                    <dt class="col-sm-3">Available Stock</dt>
                    <dd class="col-sm-9">@Model.Item.AvailableStock</dd>

                    <dt class="col-sm-3">Restock Threshold</dt>
                    <dd class="col-sm-9">@Model.Item.RestockThreshold</dd>

                    <dt class="col-sm-3">Max Stock Threshold</dt>
                    <dd class="col-sm-9">@Model.Item.MaxStockThreshold</dd>
                </dl>

                <div class="mt-3">
                    <a asp-page="Edit" asp-route-id="@Model.Item.Id" class="btn btn-warning">Edit</a>
                    <a asp-page="Index" class="btn btn-secondary">Back to List</a>
                </div>
            </div>
        </div>
    }
    else
    {
        <p>Item not found.</p>
    }
</div>
